# youtube-etl-project
nd-to-end YouTube Data Pipeline (ETL) project that extracts trending video data using the YouTube API, transforms it using Python (Pandas), stores it in MySQL, and visualizes insights through an interactive Power BI dashboard.
